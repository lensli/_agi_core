.pdb 为调试信息

cargo new --vcs=git 可以在git 项目下 再初始化一个git项目

cargo new {project name}
cargo build
cargo run 
cargo check
cargo build release 



let mut x = 10:
let y = &mut x:
println!("y={},x={}",*y,x);
//可变借用对象y 和不可变借用x 不能同时存在  需要等y的生命周期结束再使用x
